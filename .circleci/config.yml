version: 2.1

orbs:
  maven: circleci/maven@0.0.12

workflows:
  code_compiling:
    jobs:
      build:
        docker:
          - image: cimg/openjdk:15.0.0
        steps:
          - checkout
          - run: java --version
  maven_test:
    jobs:
      - maven/test